---
// src/pages/index.astro
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>burdbone/goodeye</title>
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        background: url('/goodeyebg.png') center center / cover no-repeat;
        min-height: 100vh;
        cursor: pointer;
        position: relative;
      }

      .logo-container {
        position: fixed;
        top: 40px;
        right: 40px;
        width: 150px;
        height: 150px;
        perspective: 1000px;
      }

      .logo-container img {
        width: 100%;
        height: 100%;
        object-fit: contain;
        animation: spinHorizontal 3s linear infinite;
        transform-style: preserve-3d;
      }

      @keyframes spinHorizontal {
        from {
          transform: rotateY(0deg);
        }
        to {
          transform: rotateY(360deg);
        }
      }

      /* Click anywhere hint */
      .hint {
        position: fixed;
        bottom: 40px;
        left: 50%;
        transform: translateX(-50%);
        color: #FFF;
        font-family: 'Work Sans', sans-serif;
        font-size: 12px;
        text-transform: uppercase;
        letter-spacing: 0.2em;
        opacity: 0.8;
        animation: fade 2s ease-in-out infinite;
      }

      @keyframes fade {
        0%, 100% { opacity: 0.3; }
        50% { opacity: 0.7; }
      }
    </style>
  </head>
  <body onclick="window.location.href='/home'">
    <div class="logo-container">
      <!-- Replace this src with your actual logo path -->
      <img src="/gman2.png" alt="burdbone/goodeye logo" />
    </div>
    <div class="hint">CLICK TO ENTER</div>

    <script>
      // Auto-redirect after 3 seconds (optional)
      // setTimeout(() => {
      //   window.location.href = '/home';
      // }, 3000);
    </script>
  </body>
</html>