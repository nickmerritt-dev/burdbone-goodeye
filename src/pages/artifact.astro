---
// src/pages/artifact.astro
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>Artifacts - burdbone/goodeye</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Work+Sans:wght@400;700;900&display=swap" rel="stylesheet">
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family: 'Work Sans', sans-serif;
        background: #F5F5F0;
        color: #000;
        line-height: 1.2;
        padding-bottom: 80px;
      }

      /* Top Navigation Bar */
      .top-nav {
        background: #F5F5F0;
        border-bottom: 3px solid #000;
        padding: 0;
        display: flex;
        justify-content: space-between;
        align-items: stretch;
      }

      .top-nav-left {
        display: flex;
        border-right: 3px solid #000;
      }

      .nav-item {
        padding: 20px 30px;
        border-right: 3px solid #000;
        text-decoration: none;
        color: #000;
        font-size: 13px;
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 0.05em;
        transition: background 0.2s;
        display: flex;
        align-items: center;
      }

      .nav-item:hover {
        background: #000;
        color: #F5F5F0;
      }

      .nav-item:last-child {
        border-right: none;
      }

      /* Page Header */
      .page-header {
        padding: 60px;
        border-bottom: 3px solid #000;
        text-align: center;
      }

      .page-title {
        font-size: clamp(40px, 8vw, 80px);
        font-weight: 900;
        text-transform: uppercase;
        letter-spacing: -0.02em;
      }

      /* Design Grid */
      .design-grid {
        padding: 40px;
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 40px;
        align-items: start;
      }

      .design-item {
        display: block;
        text-decoration: none;
        color: #000;
        transition: transform 0.2s;
      }

      .design-item:hover {
        transform: scale(1.02);
      }

      .design-image {
        width: 100%;
        height: auto;
        border: 3px solid #000;
        display: block;
        background: #fff;
      }

      /* Spotify Player */
      .spotify-player {
        position: fixed;
        bottom: 0;
        left: 0;
        right: 0;
        background: #F5F5F0;
        z-index: 1000;
      }

      .spotify-player iframe {
        display: block;
        width: 100%;
      }

      @media (max-width: 768px) {
        .design-grid {
          grid-template-columns: 1fr;
          padding: 20px;
          gap: 30px;
        }

        .page-header {
          padding: 40px 20px;
        }

        .top-nav {
          flex-direction: column;
        }

        .top-nav-left {
          border-right: none;
          border-bottom: 3px solid #000;
        }

        .nav-item {
          border-bottom: 3px solid #000;
          border-right: none;
        }
      }
    </style>
  </head>
  <body>
    <nav class="top-nav">
      <div class="top-nav-left">
        <a href="/" class="nav-item">Home</a>
        <a href="/artifact" class="nav-item">Artifact</a>
        <a href="#" class="nav-item">Contact</a>
      </div>
      <a href="https://are.na/burdbone-goodeyemedia" class="nav-item" target="_blank">Are.na</a>
    </nav>

    <div class="page-header">
      <h1 class="page-title">ARTIFACTS</h1>
    </div>

    <div class="design-grid">
      <!-- Add your design images here -->
      <!-- Example: -->
      <!-- <a href="/path-to-full-image.jpg" class="design-item" target="_blank">
        <img src="/path-to-image.jpg" alt="Design name" class="design-image" />
      </a> -->
      
      <!-- Placeholder examples - replace with your actual files -->
      <a href="/gman.png" class="design-item" target="_blank">
        <img src="/gman.png" alt="G Man Design" class="design-image" />
      </a>
      
      <a href="/OG-g(solid).png" class="design-item" target="_blank">
        <img src="/OG-g(solid).png" alt="OG G Solid" class="design-image" />
      </a>
    </div>

    <!-- Spotify Player -->
    <div class="spotify-player">
      <iframe data-testid="embed-iframe" style="border-radius:12px" src="https://open.spotify.com/embed/playlist/0pJDXvZwOAYgySploeeXZQ?utm_source=generator" width="100%" height="80" frameBorder="0" allowfullscreen="" allow="autoplay; clipboard-write; encrypted-media; fullscreen; picture-in-picture" loading="lazy"></iframe>
    </div>
  </body>
</html>